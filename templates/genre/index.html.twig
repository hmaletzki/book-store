{% extends 'base.html.twig' %}

{% block title %}Hello GenreController!{% endblock %}

{% block body %}
    <div class="headline">
        <h1>{{ genre.name }}</h1>
    </div>
    <div class="content">
        {% include 'book/_books_table.html.twig' with {'books': genre.books} %}

        <a href="{{ path('home') }}"><button>Back to Homepage</button></a>
    </div>
{% endblock %}
